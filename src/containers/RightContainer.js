import React from "react";

const RightContainer = props => {
  return <div style={rightContainer} />;
};

const rightContainer = {
  width: "25%",
  height: "100%",
  borderLeftStyle: "solid",
  borderColor: "#212529",
  borderWidth: .5,
  right: 0
};

export default RightContainer;
