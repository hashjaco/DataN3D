import React, { Component } from 'react'
import {  } from 'react-three-fiber'

const Point = props => {

  const setToSelected = () => ({

  })

  return(
    <mesh
      onClick={setToSelected()}
    >

    </mesh>
  )
}